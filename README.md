# zero-nvim

## 前置依赖
1. 安装node.js

```
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash

# 重新加载 shell 配置
source ~/.zshrc

# 安装最新的 LTS 版本
nvm install --lts

# 使用该版本
nvm use --lts
```

## 更新插件
进入neovim，执行:Lazy update
